networks:
  panoseti-net:
    driver: bridge

services:
  # 1. The Log Aggregator
  loki:
    image: grafana/loki:2.9.0
    ports:
      - "3100:3100"
    command: -config.file=/etc/loki/local-config.yaml
    networks:
      - panoseti-net

  # 2. Redis (The Buffer)
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    networks:
      - panoseti-net

  # 3. Visualization
  grafana:
    image: grafana/grafana:latest
    ports:
      - "3000:3000"
    environment:
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
    volumes:
      # Mount your provisioning files here if you have them
      - ./grafana_provisioning:/Users/nico/panoseti/panoseti_grafana/grafana_provisioning/
    networks:
      - panoseti-net